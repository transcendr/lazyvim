return {
  {
    "mfussenegger/nvim-dap",
    lazy = false,
    -- opts = function(_, opts)
    --   opts.mapping = vim.tbl_extend("force", opts.mapping, require("config.dap-go-mappings").dap)
    -- end,
  },
}
